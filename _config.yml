# Site settings
title: Valentina Krauß
email: kontakt@valentina-krauss.de
description: >- # this means to ignore newlines until "baseurl:"
  Valentina Krauß – Hochzeits- und Eventplanung sowie einfühlsame
  Hochzeits- und Trauerreden. Für Momente, die in Erinnerung bleiben.
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol, e.g. https://example.com

# Kontakt & Social
phone: "+49 170 1234567"
instagram_username: valentina.krauss.events
email_display: kontakt@valentina-krauss.de

# Build settings
markdown: kramdown

# The Creative theme is vendored directly into this repo (see _layouts,
# _includes, _sass, css/, js/, img/, fonts/, font-awesome/), so no `theme`
# or `remote_theme` key is needed. Content lives in the _includes/*.html
# partials — edit those to change the page sections.

# Exclude from processing.
exclude:
  - Gemfile
  - Gemfile.lock
  - vendor/
  - .sass-cache/
  - .jekyll-cache/
  - README.md
